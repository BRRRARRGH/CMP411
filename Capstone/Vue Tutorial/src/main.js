import { createApp } from 'vue' //allows the instantiation of apps in vue
import './style.css' //connects the stype page
import App from './App.vue' //connects the js to the vue APP

createApp(App).mount('#app')
// AI --> Basically initializes an app and connects to html of page
